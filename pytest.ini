[pytest]
testpaths = tests
markers =
    skills_benchmark: external/model-backed Skills Executor benchmark tests
    skills_real_execution: external/model-backed Skills Executor real execution benchmark tests
