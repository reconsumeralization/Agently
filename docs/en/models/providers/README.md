# Provider Recipes

Start with [`index.md`](index.md). This directory lists `Agently.set_settings(...)` shapes per provider.

Note: these docs pin the Agently-side fields and protocol plugin choice. Fill the concrete `model` value from the provider's current official docs and your account's available model list.
