# Services

建议按这个顺序读：

1. [FastAPI 服务封装](fastapi.md)：把 agent、request provider 或 TriggerFlow execution 暴露为 HTTP、SSE 或 WebSocket。

这个文件夹讨论服务边界。runtime event（运行时事件）归 [Observability](../observability/)；多阶段执行归 [TriggerFlow](../triggerflow/)。
